- When you create a channel it is setting the name as the channel ID and the description as the name
- The report button is not visible to normal users
- The bans and timeouts do not do anything
- There is no way to report a users bio, username, or display name
- When you send direct messages they do not send and the conversation does not show up in the channel list
- Display name changes do not show up until the page is reloaded
- There are not any customization options for the app and the config.json is unused
- There are no staff only channels
- There is no ways to remove a ban once it has been applied
- The SQLite database does not have any form of health checks or automated data recovery
- If the SQLite database files are deleted the app breaks and there is no way to rebuild it without doing it manually
- The documentation needs to be updated as it is outdated